package jeu;

public interface Fenetre {
	/** Envoie un message vers le serveur
	 * 
	 * @param texte		Le texte envoyé
	 * @throws IOException
	 */
	//public void envoyerMessage(String texte) throws IOException;

	/** Mise à jour de la liste des utilisateurs connectés
	 * 
	 * @param pseudos		La nouvelle liste de pseudos
	 * @param texte			Le message de connexion/déconnexion
	 * @param color			La couleur avec laquelle afficher le message
	 */
	//public void majPseudos(HashMap<String, Color> pseudos, String texte, Color color);
}